/** @type {import('next').NextConfig} */
const nextConfig = {
  // Si tenías alguna configuración especial aquí, puedes mantenerla.
  // Por ejemplo, si tenías transpilePackages: ['ai'], déjalo.
};

module.exports = nextConfig;
